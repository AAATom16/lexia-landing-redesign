{
  "name": "lexia-web",
  "version": "1.0.0",
  "private": true,
  "description": "Lexia static web",
  "scripts": {
    "build": "echo 'static site, no build'",
    "start": "serve . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
